<div>
  <h2><%= @restaurant.name %></h2>

  <div>
    <p><%= @restaurant.address %></p>
  </div>
  <p class="link-back-index">
  <%= link_to "Back to My Restaurants", restaurants_path %>
  </p>
</div>

<!-- <div>
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <#%= f.input :content %>
    <#%= f.input :rating %>
    <%= f.submit %>
  <#% end %>
</div>
 -->
